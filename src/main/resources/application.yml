spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB10Dialect
    show-sql: true

app:
  security:
    jwt:
      keystore-location: jwt-keystore.jks
      keystore-password: root1234
      key-alias: jwt-sign-key
      private-key-passphrase: root1234